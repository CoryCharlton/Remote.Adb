namespace Remote.Adb.Desktop.Devices;

/// <summary>Whether a device row represents a virtual device (an AVD) or a physical/wireless device.</summary>
public enum DeviceKind
{
    Virtual,
    Physical,
}
